const root = "/";
const connect = `${root}connect`;
const analytics = `${root}analytics`;
const configure = `${root}configure`;

export const ROUTES = {
  root,
  connect,
  analytics,
  configure,
};
